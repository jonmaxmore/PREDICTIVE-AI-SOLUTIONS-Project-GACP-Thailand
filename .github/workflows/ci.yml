name: ci

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: gacp
          POSTGRES_PASSWORD: gacp
          POSTGRES_DB: gacp_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U gacp"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      GACP_ENV: test
      GACP_DATABASE_URL: postgresql://gacp:gacp@localhost:5432/gacp_test
      GACP_DATABASE_DIRECT_URL: postgresql://gacp:gacp@localhost:5432/gacp_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: corepack enable
      - run: corepack pnpm install --frozen-lockfile
      - run: corepack pnpm check
      - run: corepack pnpm test
      - run: corepack pnpm db:migrate
      - run: corepack pnpm test:integration
      - run: corepack pnpm build
